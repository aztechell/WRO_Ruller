{
  "name": "wro-ruller-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:maps": "node scripts/sync-maps-scaled.mjs",
    "dev": "npm run sync:maps && vite",
    "build": "npm run sync:maps && tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run --globals",
    "test:watch": "vitest --globals"
  },
  "dependencies": {
    "sharp": "^0.34.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^2.1.8"
  }
}
